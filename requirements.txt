# Minimal requirements for Streamlit Cloud compatibility
streamlit
pandas
plotly

# Basic utilities that should always work
python-dotenv
PyPDF2
python-docx

# Optional: Try LangChain but don't fail if it doesn't work
# langchain
# langchain-openai
# langchain-community
# pydantic
